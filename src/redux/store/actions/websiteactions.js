export const addWebsiteAction = (website) => async (dispatch) => {
  // const data = await loginCandidate(userCredentials);
  // dispatch(setUserAction(data));
  // if (data.errorMessage === '') {
  // 	dispatch(DataActions.loadCities());
  // }
};
